server:
  port: 8030
spring:
  application:
    name: zuul-gateway
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
zuul:
  routes:
    user-server: /user/**
    home-server: /home/**
ribbon:
  ReadTimeout: 10000  #请求处理的超时时间
  ConnectTimeout: 10000  #请求连接的超时时间