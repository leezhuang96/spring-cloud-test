server:
  port: 8020
spring:
  application:
    name: home-server
  sleuth:
    web:
      client:
        enabled: true
    sampler:
      probability: 1.0
    zipkin:
      base-url: http://localhost:8763/
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/